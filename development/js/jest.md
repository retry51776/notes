# Jest

```js
describe('XYZ', () => {
    it.todo('Test that never builded');

    it('test 1', () => {
        expect(1).toBe(1);
    });
    it('test 2', () => {
        expect({}).toEqual({});
    });
}) 
```